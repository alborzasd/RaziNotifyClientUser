import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    direction: 'rtl', // only works for ios ?
    flex: 1,
    // padding: 10,
  },
});

export default styles;
